# main branch = deployment defaults; dev branch has localhost defaults for
# local development instead. Update this once the Render backend URL is
# confirmed (defaults to the service name if unclaimed).
VITE_API_BASE_URL=https://vc-backend.onrender.com
VITE_APP_NAME=ViaCerta
VITE_PARENT_FLOW_ENABLED=true
VITE_SENTRY_DSN=
